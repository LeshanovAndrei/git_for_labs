Tasks:
*resolved*1. Решение проблемы цвета, hwnd, hdc

2. Методы класса Current

*resolved*3. Написать отдельные Draw() для каждой фигуры
	3.1 Оттестировать методы

4. На основе цветного Draw() остальные методы

5. Реализовать управление объектом
(Выглядит довольно сложным одновременно и ловить клавиши
и считывать набранную в консоли циферку)

6. Методы агрегата

7. Конструкторы и деструкторы всех фигур

8. Придумать и реализовать след объекта. 
	(предлагаю след вести из центра, а в Shape сделать bool track, отвечающий за это, и if(track) в Draw() рисовать еще и точку в центре)

Resolved:
1. Цвет - передавать как три int значения в метод Draw(), цвета для всех задач - захардкодить
hwnd и hdc - сделать глобальными. Их инициализировать и освободить в main();

3. Круг написан при помощи Ellipse(), квадрат - Rectangle(), треугольник - PolyDraw()
У всех фигур радиус - описанной окружности.